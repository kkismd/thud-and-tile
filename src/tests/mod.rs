use super::*;
use tetromino::TetrominoShape;

// mod board_logic_tests; // clear_lines()を使用しているため一時的に無効化
mod color_consistency_tests;
// mod debug_score_tests; // clear_lines()を使用しているため一時的に無効化
mod game_state_tests;
mod rotation_tests;
